import Layout from '../components/layout/Layout';

function NotFoundPage() {
  return (
    <Layout>
      <div
        style={{
          textAlign: 'center',
          fontSize: 40,
        }}
      >
        404 | Not found page
      </div>
    </Layout>
  );
}

export default NotFoundPage;
